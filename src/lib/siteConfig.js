// Single source of truth for the site's canonical origin — used by
// sitemap.xml, canonical link tags, and Open Graph URLs so it can't drift.
export const SITE_URL = 'https://pixelscry.com';
export const SITE_NAME = 'PixelScry';
